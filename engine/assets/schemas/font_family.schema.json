{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "core://schemas/font_family.schema.json",
  "title": "Toast Font Family (.tff)",
  "description": "Ordered list of font faces registered under one family name",
  "type": "object",
  "required": ["name"],
  "additionalProperties": false,
  "properties": {
    "name":  { "type": "string", "x-toast-type": "string", "default": "Unnamed Family", "description": "Family name referenced from RCSS font-family" },
    "fonts": { "type": "array",  "x-toast-type": "asset[]", "x-toast-asset-type": "font", "default": [], "description": "Font faces loaded into the family" }
  }
}
